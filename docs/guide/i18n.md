# Internationalization (i18n)

Super UI has built-in support for multiple languages.

## Supported Languages

- 🇬🇧 English
- 🇺🇿 Uzbek (O'zbek)

## Usage

```tsx
import { LanguageSwitcher } from '@/components/ui/language-switcher'

<LanguageSwitcher />
```

## Adding Translations

Super UI uses i18next for internationalization.

Translation files are located in `src/i18n/locales/`.

### Example

```json
// en.json
{
  "welcome": "Welcome",
  "hello": "Hello {{name}}"
}
```

```json
// uz.json
{
  "welcome": "Xush kelibsiz",
  "hello": "Salom {{name}}"
}
```

## Using Translations

```tsx
import { useTranslation } from 'react-i18next'

function MyComponent() {
  const { t } = useTranslation()
  
  return <h1>{t('welcome')}</h1>
}
```

## More Details

See the i18next documentation for advanced usage.
